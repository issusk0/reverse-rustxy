[package]
name = "reverse-rustxy"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest={version= "0.12.25", features=["blocking"]}
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml="0.9.7"
